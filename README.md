# react_terminal
Testing React/Flux for web terminal app

Steps to install locally

A. start python server (Flask)
   $ virtualenv venv
   $ . venv/bin/activate
   $ python server.py

B. Build Front-end
   $ npm instal
   $ gulp


